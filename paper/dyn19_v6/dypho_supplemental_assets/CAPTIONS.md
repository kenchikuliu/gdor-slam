# Suggested Captions

**Supplemental Table II.** Ordered DYN-19 module configurations on TUM
`fr3/w/xyz` and `fr3/w/half`. Values are local ATE RMSE means and population
standard deviations across seeds 0/1/2. Lower is better. The rows are complete
ordered configurations, not factorial estimates of independent module effects.

**Supplemental Table III.** Broad runtime context. Rows marked `[ext]` are
copied from the registered DyPho-SLAM runtime table and were reported on mixed
hardware and timing scopes. The DYN-19 Full row reports local application
end-to-end throughput across 30 runs; no tracking/mapping component split is
inferred. The table is descriptive and should not be used for cross-hardware
speed ranking.
